"use server"
import {ContactAgentFormData} from "@/features/contact/types/form";

export async function sendContactMessage(contact: ContactAgentFormData): Promise<void> {
//TODO: Post data to the api
}